package com.ternary.dao;

import java.sql.SQLException;
import java.util.List;

import com.ternary.model.Request;

public interface RequestDao {

	// CRUD OPERTAIONS
	public boolean insertCompleteRequest(Request request);
	
	// eventlocation, event, grade, request
	public boolean insertEventLocation(Request request);
	public boolean insertEvent(Request request);
	public boolean insertGrade(Request request);
	public boolean insertRequest(Request request);

	public List<Request> getRequests(int empId);
}
